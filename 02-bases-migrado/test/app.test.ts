import { describe, expect, test } from "@jest/globals";

describe("Test in the App File", () => {
  test("should be true", () => {
    // 1. Arrange

    // 2. Act

    // 3.Assert
    expect(true).toBe(true);
  });
});
